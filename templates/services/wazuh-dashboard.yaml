apiVersion: v1
kind: Service
metadata:
  name: wazuh-dashboard
  labels:
    app: wazuh-dashboard
spec:
  selector:
    app: wazuh-dashboard
  ports:
    - name: http
      port: 9300
      targetPort: 9300
  clusterIP: None