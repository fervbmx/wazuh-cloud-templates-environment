apiVersion: batch/v1
kind: Job
metadata:
  generateName: create-backup
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      containers:
      - name: create-backup
        image: velero/velero
        command:
          - 'velero'
          - 'backup'
          - 'create'
          - {{ .Release.Namespace }}-{{ now | date "2006-01-02T15:04:05" }}
          - '--include-namespaces'
          - '{{ .Release.Namespace }}'
      restartPolicy: Never
  backoffLimit: 2